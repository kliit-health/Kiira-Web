import { AuthLayout } from 'src/layouts';
// import useAuth from 'src/hooks/useAuth';

const Login = () => {
  // const { login } = useAuth();
  return <AuthLayout>hii</AuthLayout>;
};

export default Login;
