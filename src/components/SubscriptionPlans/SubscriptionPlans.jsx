import React from 'react';
import { ContentContainer } from '../shared/styledComponents';

const SubscriptionPlans = () => {
  return (
    <ContentContainer className="w-full h-full overflow-hidden">
      <ContentContainer className="bg-[#E2EDFF] h-full "></ContentContainer>
    </ContentContainer>
  );
};

export default SubscriptionPlans;
